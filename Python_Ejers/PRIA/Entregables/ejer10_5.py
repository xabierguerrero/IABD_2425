import numpy as np
adiviar = np.random.randint(1,20, size =5)
print(adiviar)
print("Introduce cinco numeros separados por coma(','):")
nums=np.array(input().split(","))
print(nums)
"""
acierto=False
while not acierto:
    for num in nums:
        num=int(num)
        if num in adiviar:
            print(num,"está")
            if nums.index(int(num)==np.ndenumerate(num):
                print(num,"es")
    acierto=True
"""
